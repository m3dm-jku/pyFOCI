.. _quick_start:

###############
Getting started
###############


Install
=======

From PyPI
---------

.. _uv: https://docs.astral.sh/uv/

`pyFOCI` is available in the PyPI repositories. You can install it via `pip`:

.. prompt:: bash $

  pip install pyFOCI

Or e.g. with `uv`_, here by creating a temporary virtual environment:

.. prompt:: bash $

  uv run --with pyFOCI python -c "import pyFOCI; print(pyFOCI.__version__)"

